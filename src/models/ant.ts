export interface Ant {
  name: string;
  color: string;
  length: number;
  weight: number;
}

export interface AntExpand {
  name: string;
  color: string;
  length: number;
  weight: number;
  state: string;
  odds: number;
}
